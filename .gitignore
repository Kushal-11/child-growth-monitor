# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual Environment
venv/
.venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Database
*.db
*.sqlite
*.sqlite3

# Uploads (user-uploaded images)
uploads/*
!uploads/.gitkeep

# Environment variables
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Test coverage
.coverage
htmlcov/
.pytest_cache/

# MediaPipe model cache (if any)
*.task.cache

# Training artifacts stay local. The small promoted runtime bundle is tracked
# so a clean checkout can run the same version in FastAPI and Flutter.
data/models/*
!data/models/weight_estimator.tflite
!data/models/wasting_classifier.tflite
!data/models/feature_scaler.json
!data/models/label_encoder.json
!data/models/model_manifest.json

# Generated synthetic training dataset (regenerated by ml/generate_synthetic_data.py)
data/training_data/

# Local notes / sample assets (not meant to be committed)
notes.txt
sample images/

# Claude Code / superpowers local runtime state
.claude/worktrees/
.superpowers/
.codex/

# Generated batch assessment output (regenerated by scripts/batch_assess.py)
data/batch_results.csv

# Field study data (child photos + medical ground truth — never commit)
field_data/
